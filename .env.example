# =====================================================
# NIREEKSHAK — SIH Phase 2 Environment Configuration
# Team: TrustUs | SIH Problem Statement: SIH26102
# =====================================================

# Database Configuration (PostgreSQL with automatic SQLite fallback)
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=trustus
# Alternative connection URI:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/trustus
# SQLite Fallback:
# DATABASE_URL=sqlite:///./nireekshak.db

# FastAPI Backend Configuration
FASTAPI_HOST=0.0.0.0
FASTAPI_PORT=8000
ENVIRONMENT=development

# Security & Authentication
JWT_SECRET_KEY=trustus_sih2026_nireekshak_secret_key_v2
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# AI / ML Model Artifacts Directory
# MODEL_DIR=../../content/nireekshak-engine-final/models

# Next.js Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:8000/api
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
